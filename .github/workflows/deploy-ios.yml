# name: Deploy iOS to TestFlight

# # Trigger on version tags (e.g. v1.2.3) or manually via GitHub UI
# on:
#   push:
#     tags:
#       - 'v[0-9]+.[0-9]+.[0-9]+'
#   workflow_dispatch:

# permissions:
#   contents: read

# jobs:
#   deploy-ios:
#     name: Build & Deploy iOS IPA to TestFlight
#     runs-on: macos-latest  # macOS required for Xcode toolchain

#     steps:
#       # ── Source ────────────────────────────────────────────────────────────
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       # ── Flutter ────────────────────────────────────────────────────────────
#       - name: Set up Flutter (stable)
#         uses: subosito/flutter-action@v2
#         with:
#           channel: 'stable'
#           cache: true

#       - name: Install Flutter dependencies
#         run: flutter pub get

#       # ── Build Flutter iOS (no codesign — Fastlane handles signing) ─────────
#       - name: Build Flutter iOS release (unsigned)
#         run: flutter build ios --release --no-codesign

#       # ── Ruby / Fastlane ────────────────────────────────────────────────────
#       - name: Set up Ruby
#         uses: ruby/setup-ruby@v1
#         with:
#           ruby-version: '3.3'
#           bundler-cache: true

#       # ── Sign & Upload via Fastlane ─────────────────────────────────────────
#       # Fastlane Match pulls certificates + profiles from your private Git repo,
#       # Fastlane Gym signs the .xcarchive, and Fastlane Pilot uploads to TestFlight.
#       - name: Deploy to TestFlight via Fastlane
#         env:
#           # App Store Connect API key (no 2FA issues in CI)
#           APP_STORE_CONNECT_API_KEY_ID:  ${{ secrets.APP_STORE_CONNECT_API_KEY_ID }}
#           APP_STORE_CONNECT_ISSUER_ID:   ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
#           APP_STORE_CONNECT_API_KEY:     ${{ secrets.APP_STORE_CONNECT_API_KEY }}
#           # Fastlane Match — private certificate repo
#           MATCH_GIT_URL:                 ${{ secrets.MATCH_GIT_URL }}
#           MATCH_GIT_BASIC_AUTH:          ${{ secrets.MATCH_GIT_BASIC_AUTH }}
#           MATCH_PASSWORD:                ${{ secrets.MATCH_PASSWORD }}
#           # Apple credentials
#           APPLE_ID:                      ${{ secrets.APPLE_ID }}
#           APP_STORE_CONNECT_TEAM_ID:     ${{ secrets.APP_STORE_CONNECT_TEAM_ID }}
#           DEVELOPER_PORTAL_TEAM_ID:      ${{ secrets.DEVELOPER_PORTAL_TEAM_ID }}
#           # Tell Fastlane this is a CI environment
#           CI: "true"
#         run: bundle exec fastlane ios beta
